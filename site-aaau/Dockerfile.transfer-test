FROM node:22-alpine

WORKDIR /app

RUN apk add --no-cache openssl

COPY package.json package-lock.json ./
COPY prisma ./prisma
RUN npm ci

COPY . .
RUN chmod +x scripts/start-transfer-test.sh

EXPOSE 3000

ENTRYPOINT ["./scripts/start-transfer-test.sh"]

